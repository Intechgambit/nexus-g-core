# Copy to docker/.env and fill in for your cloud provider

# AWS
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_SESSION_TOKEN=
AWS_DEFAULT_REGION=us-east-1

# GCP (mount key file in docker-compose.yml)
GOOGLE_APPLICATION_CREDENTIALS=/secrets/gcp.json

# Azure
AZURE_STORAGE_CONNECTION_STRING=
